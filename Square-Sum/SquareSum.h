#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
#include <vector>
using namespace std;

// check if a string contain double number
bool isDouble(const string& str);

// Read from file and calculate sum of square 
double readFile(ostream& inFile);

